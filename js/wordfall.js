(() => {
  const STORAGE_KEY = "xiaoluo-wordfall-library-v1";
  const SKIN_KEY = "xiaoluo-wordfall-skin-v1";
  const DEFAULT_SOURCE = `ability = 能力
absorb = 吸收
abstract = 抽象的
abundant = 丰富的
academic = 学术的
accept = 接受
access = 进入
accompany = 陪伴
accomplish = 完成
accurate = 准确的
achieve = 实现
acquire = 获得
adapt = 适应
adequate = 足够的
adjust = 调整
admit = 承认
advance = 前进
advantage = 优势
affect = 影响
agency = 机构
agriculture = 农业
alternative = 替代方案
ambition = 雄心
analyse = 分析
ancient = 古代的
announce = 宣布
anxious = 焦虑的
apparent = 明显的
appeal = 吸引力
appear = 出现
apply = 申请
approach = 方法
appropriate = 合适的
approve = 赞成
argue = 争论
arise = 出现
arrange = 安排
artificial = 人工的
aspect = 方面
assemble = 集合
assess = 评估
assist = 协助
assume = 假设
attach = 附上
attain = 达到
attitude = 态度
attract = 吸引
available = 可获得的
average = 平均的
aware = 意识到
balance = 平衡
barrier = 障碍
benefit = 好处
biology = 生物学
border = 边界
brief = 简短的
calculate = 计算
campaign = 活动
capacity = 容量
career = 职业
category = 类别
challenge = 挑战
character = 性格
chemical = 化学的
circumstance = 情况
civilization = 文明
clarify = 阐明
classic = 经典的
climate = 气候
cooperate = 合作
complex = 复杂的
concentrate = 专注
concept = 概念
conclude = 得出结论
conduct = 实施
confirm = 确认
conflict = 冲突
consequence = 后果
consider = 考虑
consistent = 一致的
constant = 持续的
consume = 消费
contact = 联系
contain = 包含
contribute = 贡献
convenient = 方便的
convince = 说服
cooperate = 合作
create = 创造
critical = 关键的
culture = 文化
curious = 好奇的
custom = 习惯
damage = 损害
debate = 辩论
decade = 十年
decline = 下降
define = 定义
demonstrate = 证明
department = 部门
depend = 依靠
describe = 描述
design = 设计
desire = 渴望
detail = 细节
detect = 发现
determine = 决定
develop = 发展
device = 设备
devote = 投入
diagnose = 诊断
digital = 数字的
dimension = 尺寸
direct = 直接的
disaster = 灾难
discover = 发现
display = 展示
distribute = 分发
diverse = 多样的
document = 文件
domestic = 国内的
dominant = 占主导的
dramatic = 戏剧性的
durable = 持久的
dynamic = 动态的
economy = 经济
effective = 有效的
efficient = 高效的
element = 元素
emerge = 出现
emphasis = 强调
enable = 使能够
encounter = 遇到
encourage = 鼓励
energy = 能源
enormous = 巨大的
ensure = 确保
environment = 环境
equivalent = 等同的
essential = 必要的
establish = 建立
evaluate = 评估
evidence = 证据
evolve = 发展
exact = 精确的
examine = 检查
exceed = 超过
exchange = 交换
exclude = 排除
exhibit = 展示
expand = 扩大
expect = 期待
expense = 花费
expert = 专家
explore = 探索
express = 表达
extend = 延伸
factor = 因素
feature = 特点
federal = 联邦的
flexible = 灵活的
focus = 聚焦
formal = 正式的
former = 以前的
framework = 框架
function = 功能
fundamental = 基本的
generate = 产生
global = 全球的
goal = 目标
government = 政府
gradual = 逐渐的
guarantee = 保证
harmful = 有害的
highlight = 强调
identity = 身份
illustrate = 说明
impact = 影响
implement = 实施
imply = 暗示
impose = 强加
improve = 改善
include = 包括
income = 收入
indicate = 表明
individual = 个人
inevitable = 不可避免的
infer = 推断
influence = 影响
inform = 通知
initial = 最初的
innovative = 创新的
inspect = 检查
instance = 例子
institute = 机构
intelligent = 智能的
intend = 打算
interact = 互动
interpret = 解释
interval = 间隔
invest = 投资
involve = 涉及
issue = 问题
justify = 证明
laboratory = 实验室
legal = 合法的
legislation = 法律
liberal = 自由的
location = 地点
maintain = 维持
major = 主要的
manage = 管理
manual = 手册
margin = 边缘
medical = 医学的
mental = 精神的
method = 方法
migrate = 移动
minimum = 最小的
minor = 次要的
modify = 修改
monitor = 监控
motivate = 激励
mutual = 相互的
negative = 消极的
network = 网络
normal = 正常的
objective = 目标
obtain = 获得
obvious = 明显的
occur = 发生
operate = 操作
opportunity = 机会
option = 选择
original = 原始的
outcome = 结果
participate = 参加
particular = 特别的
perceive = 察觉
period = 时期
permanent = 永久的
perspective = 观点
physical = 身体的
potential = 潜力
previous = 以前的
primary = 主要的
principle = 原则
priority = 优先事项
procedure = 程序
process = 过程
professional = 专业的
prohibit = 禁止
promote = 促进
proportion = 比例
prospect = 前景
protect = 保护
provide = 提供
publish = 出版
purchase = 购买
range = 范围
react = 反应
recover = 恢复
reduce = 减少
reflect = 反映
region = 地区
register = 注册
regulate = 管理
reject = 拒绝
release = 发布
relevant = 相关的
rely = 依赖
remove = 删除
require = 需要
research = 研究
resolve = 解决
resource = 资源
respond = 回应
restrict = 限制
retain = 保留
reveal = 揭示
review = 复习
role = 角色
route = 路线
scheme = 计划
section = 部分
secure = 安全的
select = 选择
significant = 重要的
similar = 相似的
source = 来源
specific = 特定的
stable = 稳定的
statistic = 统计
strategy = 策略
structure = 结构
submit = 提交
sufficient = 充足的
summary = 总结
survey = 调查
survive = 生存
symbol = 符号
target = 目标
technical = 技术的
technology = 技术
tendency = 趋势
theory = 理论
therefore = 因此
traditional = 传统的
transfer = 转移
transform = 改变
transport = 运输
trend = 趋势
ultimate = 最终的
undergo = 经历
uniform = 统一的
unique = 独特的
valid = 有效的
vary = 变化
vehicle = 车辆
version = 版本
visible = 可见的
voluntary = 自愿的
wealth = 财富
whereas = 然而
withdraw = 撤回
widespread = 广泛的`;

  const SKINS = [
    ["pulse", "脉冲蓝", "默认", "#58a6ff"], ["dragon", "苍龙轰鸣", "龙吟激光", "#5eead4"],
    ["plasma", "等离子", "电弧扩散", "#d8b4fe"], ["solar", "日冕", "炽热脉冲", "#fbbf24"],
    ["frost", "极光冰束", "冰晶回响", "#67e8f9"],
    ["thunder", "闪电雷霆", "跳动电弧与分叉火花", "#8b7cff", "advanced"],
    ["nebula", "星辰星云", "流动星云与上升星尘", "#ec8dff", "advanced"],
    ["lava", "熔岩烈焰", "岩浆翻滚与热浪火星", "#ff6b35", "advanced"],
    ["ice", "寒冰冰晶", "碎冰旋转与冻结白雾", "#91e7ff", "advanced"],
    ["rift", "虚空次元裂隙", "扭曲裂缝与引力黑洞", "#9a6cff", "advanced"],
    ["angel", "圣光天使", "多层圣光与金色羽毛", "#ffd66b", "advanced"],
    ["toxic", "毒素腐蚀", "腐蚀黏液与毒雾泡泡", "#9dff45", "advanced"],
    ["mech", "机械粒子炮", "金属碎片与环绕齿轮", "#b9d3e8", "advanced"],
    ["sakura", "樱花幻梦", "粉紫波光与飘舞花瓣", "#ff9ccf", "advanced"],
    ["chaos", "混沌爆炸", "彩色乱流与连续爆裂", "#ff5f8d", "advanced"]
  ];
  const ADVANCED_FX = {
    thunder: { glyphs: ["✦", "✧", "⚡"], color: "#9ca7ff", count: 22, spread: 92, duration: 640 },
    nebula: { glyphs: ["✦", "·", "✧"], color: "#f6b7ff", count: 20, spread: 82, duration: 980 },
    lava: { glyphs: ["•", "✦", "·"], color: "#ff9b43", count: 24, spread: 96, duration: 780 },
    ice: { glyphs: ["❄", "✧", "✦"], color: "#d9f8ff", count: 20, spread: 84, duration: 980 },
    rift: { glyphs: ["●", "◌", "✦"], color: "#b88cff", count: 18, spread: 74, duration: 900 },
    angel: { glyphs: ["❧", "✦", "✧"], color: "#ffe49b", count: 18, spread: 104, duration: 1050 },
    toxic: { glyphs: ["●", "◦", "✦"], color: "#c0ff68", count: 22, spread: 86, duration: 860 },
    mech: { glyphs: ["⚙", "◆", "▣"], color: "#d5edff", count: 18, spread: 72, duration: 760 },
    sakura: { glyphs: ["✿", "❀", "·"], color: "#ffb4da", count: 22, spread: 112, duration: 1120 },
    chaos: { glyphs: ["✦", "✹", "✧"], color: "#fff25e", count: 30, spread: 120, duration: 720 }
  };
  const SPEEDS = { slow: [24, "慢速"], normal: [42, "中速"], fast: [65, "快速"], extreme: [92, "极速"] };
  let activeDestroy = null;

  function parseLibrary(source) {
    const seen = new Set();
    return String(source || "").split(/\r?\n/).map((line) => {
      const [word, ...meaning] = line.split(/[=＝]/);
      return { word: String(word || "").trim().toLowerCase().replace(/[^a-z-]/g, ""), meaning: meaning.join("=").trim() || "常用词" };
    }).filter((item) => item.word.length >= 2 && !seen.has(item.word) && seen.add(item.word));
  }
  const defaultLibrary = () => parseLibrary(DEFAULT_SOURCE);
  const escapeHtml = (value) => String(value || "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", ">": "&gt;", "<": "&lt;", '"': "&quot;", "'": "&#39;" }[char]));

  function initWordfall() {
    const arena = document.querySelector("[data-wordfall-arena]");
    if (!arena || arena.dataset.wordfallReady) return;
    activeDestroy?.();
    arena.dataset.wordfallReady = "true";
    const events = new AbortController();
    const input = document.querySelector("[data-wordfall-input]");
    const startButton = document.querySelector("[data-wordfall-start]");
    const message = document.querySelector("[data-wordfall-message]");
    const lasers = document.querySelector("[data-wordfall-lasers]");
    const scoreEl = document.querySelector("[data-wordfall-score]");
    const livesEl = document.querySelector("[data-wordfall-lives]");
    const accuracyEl = document.querySelector("[data-wordfall-accuracy]");
    const speedSelect = document.querySelector("[data-wordfall-speed]");
    const speedLabel = document.querySelector("[data-wordfall-speed-label]");
    const pauseButton = document.querySelector("[data-wordfall-pause]");
    const stageAction = document.querySelector("[data-wordfall-stage-action]");
    const skinToggle = document.querySelector("[data-wordfall-skin-toggle]");
    const skinPicker = document.querySelector("[data-wordfall-skin-picker]");
    const skinModal = document.querySelector("[data-wordfall-skin-modal]");
    const libraryToggle = document.querySelector("[data-wordfall-library-toggle]");
    let libraryModal = document.querySelector("[data-wordfall-library-modal]");
    if (!libraryModal) {
      libraryModal = document.createElement("div");
      libraryModal.className = "modal wordfall-library-modal";
      libraryModal.dataset.wordfallLibraryModal = "";
      libraryModal.hidden = true;
      libraryModal.innerHTML = `<button class="modal-backdrop" type="button" data-wordfall-library-close aria-label="关闭"></button><section class="modal-card glass-card wordfall-library-card" role="dialog" aria-modal="true" aria-labelledby="wordfall-library-title"><button class="modal-close" type="button" data-wordfall-library-close aria-label="关闭">×</button><p class="mini-title">VOCABULARY LIBRARY</p><h2 id="wordfall-library-title">题库管理</h2><p>默认题库包含专升本常用高频词。每行输入一个词汇，格式为：<code>english = 中文</code>。</p><div class="wordfall-library-summary" data-wordfall-library-summary></div><textarea data-wordfall-library-text rows="11" placeholder="example = 例子&#10;ability = 能力"></textarea><div class="wordfall-library-actions"><button class="ghost-button" type="button" data-wordfall-library-append>追加词库</button><button class="danger-button" type="button" data-wordfall-library-replace>覆盖词库</button><button class="primary-button" type="button" data-wordfall-library-default>恢复默认题库</button></div></section>`;
      document.body.appendChild(libraryModal);
    }
    const libraryText = document.querySelector("[data-wordfall-library-text]");
    const librarySummary = document.querySelector("[data-wordfall-library-summary]");
    let library = parseLibrary(localStorage.getItem(STORAGE_KEY)) || [];
    if (!library.length) library = defaultLibrary();
    let skin = localStorage.getItem(SKIN_KEY) || "pulse";
    if (!SKINS.some((item) => item[0] === skin)) skin = "pulse";
    let speedKey = "normal", score = 0, lives = 2, typed = "", hits = 0, shots = 0, paused = true, lastTime = 0, spawnAt = 0, frame = 0, ended = false;
    const words = [];
    let audioContext = null;
    const api = window.XiaoLuoSupabase;
    const rankingList = document.querySelector("[data-wordfall-ranking-list]");
    const joinButton = document.querySelector("[data-wordfall-join-btn]");
    const nicknameModal = document.querySelector("[data-wordfall-nickname-modal]");
    const nicknameForm = document.querySelector("[data-wordfall-nickname-form]");
    const nicknameError = document.querySelector("[data-wordfall-nickname-error]");
    const guestTokenKey = "xiaoluo-wordfall-guest-token";
    const guestNicknameKey = "xiaoluo-wordfall-game-nickname";
    const localBestKey = "xiaoluo-wordfall-local-best";
    let session = null;
    let guestToken = localStorage.getItem(guestTokenKey);
    if (!guestToken) { guestToken = crypto.randomUUID(); localStorage.setItem(guestTokenKey, guestToken); }
    let guestRegistered = Boolean(localStorage.getItem(guestNicknameKey));
    const playerKey = () => session ? `user:${session.user.id}` : `guest:${guestToken}`;
    const rowKey = (row) => row.player_key || (row.user_id ? `user:${row.user_id}` : `guest:${row.guest_token}`);
    const updateJoinButton = () => {
      if (!joinButton) return;
      joinButton.textContent = session ? "已自动上榜" : guestRegistered ? "已上榜" : "参与排行榜";
      joinButton.disabled = Boolean(session || guestRegistered);
      joinButton.classList.toggle("is-active", Boolean(session || guestRegistered));
    };
    const renderRanking = async () => {
      if (!rankingList || !api?.isConfigured) return;
      try {
        const rows = await api.listWordfallGameRanking(50);
        const activeKey = playerKey();
        rankingList.innerHTML = rows.map((row, index) => {
          const profile = row.profile || {};
          const key = rowKey(row);
          const userId = row.user_id || (String(key).startsWith("user:") ? String(key).slice(5) : "");
          const mine = key === activeKey;
          const name = profile.display_name || row.display_name || `玩家${index + 1}`;
          const avatarStyle = profile.avatar_url ? ` style="background-image:url('${escapeHtml(profile.avatar_url)}')"` : "";
          const avatar = row.is_guest ? '<span class="game-ranking-avatar game-guest-avatar">游</span>' : `<button class="game-ranking-avatar" type="button" data-profile-user-id="${escapeHtml(userId)}" aria-label="查看${escapeHtml(name)}的资料"${avatarStyle}>${profile.avatar_url ? "" : escapeHtml(name.slice(0, 1))}</button>`;
          return `<li class="game-ranking-row${mine ? " is-me" : ""}"><span class="game-ranking-rank">${index + 1}</span>${avatar}<strong class="game-ranking-name">${escapeHtml(name)}${mine ? "（我）" : ""}</strong><span class="game-ranking-score">${Number(row.best_score || 0)}</span></li>`;
        }).join("") || '<li class="game-ranking-empty">完成一局后，这里会显示你的成绩。</li>';
      } catch (_) { rankingList.innerHTML = '<li class="game-ranking-empty">排行榜暂不可用，请稍后重试。</li>'; }
    };
    const saveScore = async () => {
      if (!api?.isConfigured || score <= 0) return;
      if (!session && !guestRegistered) {
        const localBest = Number(localStorage.getItem(localBestKey) || 0);
        if (score > localBest) localStorage.setItem(localBestKey, String(score));
        return;
      }
      try {
        if (session) await api.submitWordfallGameScore(score);
        else await api.submitGuestWordfallGameScore(guestToken, score);
        await renderRanking();
      } catch (error) { console.warn("Wordfall score save failed:", error.message); }
    };
    const playSound = (kind) => {
      try {
        audioContext ||= new (window.AudioContext || window.webkitAudioContext)();
        if (audioContext.state === "suspended") audioContext.resume();
        const now = audioContext.currentTime;
        const tone = (from, to, duration, volume = .075, type = "sine", delay = 0) => {
          const osc = audioContext.createOscillator(), gain = audioContext.createGain();
          osc.type = type; osc.frequency.setValueAtTime(from, now + delay); osc.frequency.exponentialRampToValueAtTime(Math.max(20, to), now + delay + duration);
          gain.gain.setValueAtTime(volume, now + delay); gain.gain.exponentialRampToValueAtTime(.001, now + delay + duration);
          osc.connect(gain); gain.connect(audioContext.destination); osc.start(now + delay); osc.stop(now + delay + duration + .02);
        };
        if (kind === "error") { tone(110, 65, .18, .12, "sawtooth"); return; }
        if (kind === "destroy") { tone(290, 780, .18, .09, "triangle"); return; }
        const voices = {
          thunder: () => { tone(90, 1280, .14, .11, "sawtooth"); tone(580, 120, .2, .05, "square", .04); },
          nebula: () => { tone(260, 680, .52, .06, "sine"); tone(520, 980, .45, .035, "sine", .08); },
          lava: () => { tone(72, 195, .3, .12, "sawtooth"); tone(340, 90, .16, .05, "square", .04); },
          ice: () => { tone(1260, 640, .32, .07, "triangle"); tone(1880, 1240, .24, .035, "sine", .05); },
          rift: () => { tone(54, 30, .48, .13, "sawtooth"); tone(170, 76, .4, .045, "sine", .04); },
          angel: () => { tone(523, 1046, .5, .075, "sine"); tone(784, 1568, .42, .04, "triangle", .08); },
          toxic: () => { tone(130, 82, .24, .11, "square"); tone(310, 165, .32, .04, "sine", .06); },
          mech: () => { tone(160, 520, .2, .1, "square"); tone(780, 250, .26, .045, "sawtooth", .03); },
          sakura: () => { tone(690, 1010, .46, .06, "sine"); tone(1170, 1350, .34, .03, "triangle", .1); },
          chaos: () => { tone(70, 740, .18, .1, "sawtooth"); tone(920, 135, .18, .065, "square", .05); tone(320, 1120, .15, .045, "triangle", .1); }
        };
        if (kind === "shot" && voices[skin]) voices[skin]();
        else tone(skin === "dragon" ? 100 : 420, skin === "dragon" ? 210 : 680, .16, .09, skin === "dragon" ? "sawtooth" : "sine");
      } catch (_) {}
    };
    const updateHud = () => { scoreEl.textContent = score; livesEl.textContent = `${lives} / 2`; accuracyEl.textContent = `${shots ? Math.round(hits / shots * 100) : 100}%`; speedLabel.textContent = SPEEDS[speedKey][1]; };
    const currentTarget = () => words.slice().sort((a, b) => b.y - a.y)[0];
    const drawTyped = () => words.forEach((entry) => entry.element.classList.toggle("is-target", entry === currentTarget()));
    const makeWord = () => {
      // One centered lane plus a vertical safety gap keeps every word readable.
      if (words.length >= 4 || ended || words.some((entry) => entry.y < 118)) return;
      const choice = library[Math.floor(Math.random() * library.length)];
      if (!choice) return;
      const element = document.createElement("article");
      element.className = "wordfall-word";
      element.innerHTML = `<strong>${escapeHtml(choice.word)}</strong><small>${escapeHtml(choice.meaning)}</small>`;
      const lanes = [24, 38, 50, 62, 76];
      const x = lanes[Math.floor(Math.random() * lanes.length)];
      const entry = { ...choice, x, y: -10, element };
      element.style.left = `${x}%`; arena.appendChild(element); words.push(entry); drawTyped();
    };
    const removeWord = (entry, reason = "destroy") => {
      const index = words.indexOf(entry); if (index >= 0) words.splice(index, 1);
      entry.element.classList.add(reason === "destroy" ? "is-destroyed" : "is-missed");
      setTimeout(() => entry.element.remove(), 360);
      typed = ""; input.value = ""; drawTyped();
    };
    const spawnAdvancedFx = (target, startX, startY, endX, endY, ok) => {
      const fx = ADVANCED_FX[skin];
      if (!fx || !ok) return;
      const fragments = fx.count;
      for (let index = 0; index < fragments; index += 1) {
        const particle = document.createElement("i");
        const alongBeam = Math.random() < .62 ? Math.random() : .92;
        const baseX = startX + (endX - startX) * alongBeam;
        const baseY = startY + (endY - startY) * alongBeam;
        const angle = Math.random() * Math.PI * 2;
        const force = (Math.random() * .7 + .3) * fx.spread;
        particle.className = `wordfall-fx-particle fx-${skin}`;
        particle.textContent = fx.glyphs[index % fx.glyphs.length];
        particle.style.setProperty("--fx-x", `${baseX}px`);
        particle.style.setProperty("--fx-y", `${baseY}px`);
        particle.style.setProperty("--fx-dx", `${Math.cos(angle) * force}px`);
        particle.style.setProperty("--fx-dy", `${Math.sin(angle) * force}px`);
        particle.style.setProperty("--fx-delay", `${Math.random() * 90}ms`);
        particle.style.setProperty("--fx-duration", `${fx.duration + Math.random() * 260}ms`);
        particle.style.setProperty("--fx-color", fx.color);
        particle.style.setProperty("--fx-size", `${10 + Math.random() * 17}px`);
        lasers.appendChild(particle);
        setTimeout(() => particle.remove(), fx.duration + 430);
      }
      const impact = document.createElement("i");
      impact.className = `wordfall-impact fx-${skin}`;
      impact.style.setProperty("--impact-x", `${endX}px`);
      impact.style.setProperty("--impact-y", `${endY}px`);
      impact.style.setProperty("--fx-color", fx.color);
      lasers.appendChild(impact);
      setTimeout(() => impact.remove(), fx.duration + 220);
    };
    const fireLaser = (ok, target = currentTarget()) => {
      const beam = document.createElement("i"); beam.className = `wordfall-laser skin-${skin}${ok ? "" : " is-error"}`;
      const arenaBox = arena.getBoundingClientRect();
      const shipBox = arena.querySelector("[data-wordfall-ship]").getBoundingClientRect();
      const targetBox = target?.element.getBoundingClientRect();
      const startX = shipBox.left - arenaBox.left + shipBox.width / 2;
      const startY = shipBox.top - arenaBox.top + 8;
      const endX = targetBox ? targetBox.left - arenaBox.left + targetBox.width / 2 : startX;
      const endY = targetBox ? targetBox.top - arenaBox.top + targetBox.height / 2 : 38;
      const deltaX = endX - startX;
      const deltaY = endY - startY;
      beam.style.setProperty("--laser-start-x", `${startX}px`);
      beam.style.setProperty("--laser-bottom", `${Math.max(0, arena.clientHeight - startY)}px`);
      beam.style.setProperty("--laser-length", `${Math.max(42, Math.hypot(deltaX, deltaY))}px`);
      beam.style.setProperty("--laser-angle", `${Math.atan2(deltaX, -deltaY) * 180 / Math.PI}deg`);
      lasers.appendChild(beam); spawnAdvancedFx(target, startX, startY, endX, endY, ok); setTimeout(() => beam.remove(), 700);
    };
    const loseLife = (reason) => {
      lives -= 1; typed = ""; input.value = ""; playSound("error"); updateHud();
      message.textContent = "漏掉单词，失去一次机会";
      arena.classList.remove("is-shaking"); void arena.offsetWidth; arena.classList.add("is-shaking");
      if (lives <= 0) { ended = true; paused = true; pauseButton.textContent = "重新开始"; stageAction.hidden = false; stageAction.textContent = "重新开始"; message.textContent = `本局 ${score} 分，点击重新开始继续训练`; void saveScore(); }
    };
    const finishWord = (entry) => { score += 2; removeWord(entry); playSound("destroy"); message.textContent = `命中 ${entry.word}，+2 分`; updateHud(); setTimeout(makeWord, 260); };
    const acceptLetter = (letter) => {
      if (paused || ended || !/^[a-z]$/.test(letter)) return;
      const target = currentTarget(); if (!target) return;
      shots += 1;
      const next = target.word[typed.length];
      if (letter === next) {
        typed += letter; hits += 1; input.value = typed; fireLaser(true); playSound("shot");
        target.element.querySelector("strong").innerHTML = `<em>${escapeHtml(typed)}</em>${escapeHtml(target.word.slice(typed.length))}`;
        if (typed === target.word) finishWord(target); else { updateHud(); }
      } else {
        fireLaser(false, target);
        playSound("error");
        typed = ""; input.value = "";
        target.element.querySelector("strong").textContent = target.word;
        message.textContent = "字母不匹配，已从第一个字母重新开始";
        updateHud();
      }
    };
    const setMessage = (text, centered = false) => { message.textContent = text; message.classList.toggle("is-center", centered); };
    const reset = () => { words.splice(0).forEach((entry) => entry.element.remove()); score = 0; lives = 2; typed = ""; hits = 0; shots = 0; ended = false; paused = false; spawnAt = 0; pauseButton.textContent = "暂停"; startButton.textContent = "重新开始"; stageAction.hidden = true; setMessage("开始训练，直接输入字母即可发射激光"); updateHud(); makeWord(); input.focus(); };
    const loop = (time) => {
      const delta = Math.min(40, time - (lastTime || time)); lastTime = time;
      if (!paused) {
        const height = arena.clientHeight;
        words.slice().forEach((entry) => {
          entry.y += SPEEDS[speedKey][0] * delta / 1000;
          entry.element.style.transform = `translate(-50%, ${entry.y}px)`;
          if (entry.y + entry.element.offsetHeight >= height) { removeWord(entry, "miss"); loseLife("miss"); }
        });
        if (time >= spawnAt) { makeWord(); spawnAt = time + Math.max(720, 1800 - SPEEDS[speedKey][0] * 9); }
        drawTyped();
      }
      frame = requestAnimationFrame(loop);
    };
    skinPicker.innerHTML = SKINS.map(([id, name, effect, color, tier]) => `<button class="${tier === "advanced" ? "is-advanced" : ""}" type="button" data-wordfall-skin="${id}" style="--skin-color:${color}"><i></i><span><strong>${name}${tier === "advanced" ? " <em>进阶</em>" : ""}</strong><small>${effect}</small></span></button>`).join("");
    const setSkin = (value) => { skin = value; localStorage.setItem(SKIN_KEY, skin); skinPicker.querySelectorAll("button").forEach((button) => button.classList.toggle("active", button.dataset.wordfallSkin === skin)); skinToggle.textContent = `激光：${SKINS.find((item) => item[0] === skin)?.[1] || "脉冲蓝"}`; };
    const openLibrary = () => { libraryText.value = library.map((item) => `${item.word} = ${item.meaning}`).join("\n"); librarySummary.textContent = `当前题库共 ${library.length} 个词汇。`; libraryModal.hidden = false; libraryModal.classList.add("open"); };
    const saveLibrary = (mode) => { const incoming = parseLibrary(libraryText.value); if (!incoming.length) return; const map = new Map((mode === "append" ? library : []).map((item) => [item.word, item])); incoming.forEach((item) => map.set(item.word, item)); library = [...map.values()]; localStorage.setItem(STORAGE_KEY, library.map((item) => `${item.word} = ${item.meaning}`).join("\n")); librarySummary.textContent = `已保存，共 ${library.length} 个词汇。`; message.textContent = "题库已更新，新的单词会立即加入下落队列"; };
    let libraryUiBound = false;
    const setupLibraryManagement = (isAdmin) => {
      if (!isAdmin) {
        // 普通用户不展示题库管理，也不保留可通过页面事件触发的管理入口。
        libraryToggle?.remove();
        libraryModal?.remove();
        return;
      }
      if (!libraryToggle || libraryUiBound) return;
      libraryUiBound = true;
      libraryToggle.hidden = false;
      libraryToggle.addEventListener("click", openLibrary, { signal: events.signal });
      document.querySelectorAll("[data-wordfall-library-close]").forEach((button) => button.addEventListener("click", () => { libraryModal.classList.remove("open"); libraryModal.hidden = true; }, { signal: events.signal }));
      document.querySelector("[data-wordfall-library-append]")?.addEventListener("click", () => saveLibrary("append"), { signal: events.signal });
      document.querySelector("[data-wordfall-library-replace]")?.addEventListener("click", () => saveLibrary("replace"), { signal: events.signal });
      document.querySelector("[data-wordfall-library-default]")?.addEventListener("click", () => { library = defaultLibrary(); localStorage.removeItem(STORAGE_KEY); libraryText.value = library.map((item) => `${item.word} = ${item.meaning}`).join("\n"); librarySummary.textContent = `已恢复默认题库，共 ${library.length} 个词汇。`; }, { signal: events.signal });
    };
    setSkin(skin); updateHud(); frame = requestAnimationFrame(loop);
    speedSelect.addEventListener("change", () => { speedKey = speedSelect.value; updateHud(); }, { signal: events.signal });
    // 开始/重新开始按钮使用委托，避免 SPA 重绘后按钮仍引用旧节点。
    const startGameFromControl = (event) => {
      const button = event.target.closest("[data-wordfall-start], [data-wordfall-stage-action]");
      if (!button || !arena.contains(button)) return;
      event.preventDefault();
      event.stopPropagation();
      if (button.matches("[data-wordfall-stage-action]") && !ended && words.length) {
        pauseButton.click();
        return;
      }
      reset();
    };
    arena.addEventListener("click", startGameFromControl, { signal: events.signal });
    startButton?.addEventListener("click", (event) => {
      event.preventDefault();
      reset();
    }, { signal: events.signal });
    pauseButton.addEventListener("click", () => { if (ended) return reset(); paused = !paused; pauseButton.textContent = paused ? "继续" : "暂停"; stageAction.hidden = !paused; stageAction.textContent = "继续游戏"; setMessage(paused ? "已暂停，点击继续或按空格恢复" : "继续训练"); if (!paused) input.focus(); }, { signal: events.signal });
    skinToggle.addEventListener("click", () => { skinModal.hidden = false; skinModal.classList.add("open"); }, { signal: events.signal });
    document.querySelectorAll("[data-wordfall-skin-close]").forEach((button) => button.addEventListener("click", () => { skinModal.classList.remove("open"); skinModal.hidden = true; }, { signal: events.signal }));
    skinPicker.addEventListener("click", (event) => { const button = event.target.closest("[data-wordfall-skin]"); if (!button) return; setSkin(button.dataset.wordfallSkin); skinModal.classList.remove("open"); skinModal.hidden = true; }, { signal: events.signal });
    joinButton?.addEventListener("click", () => { nicknameModal.hidden = false; nicknameModal.classList.add("open"); }, { signal: events.signal });
    document.querySelectorAll("[data-wordfall-nickname-close]").forEach((button) => button.addEventListener("click", () => { nicknameModal.classList.remove("open"); nicknameModal.hidden = true; }, { signal: events.signal }));
    nicknameForm?.addEventListener("submit", async (event) => {
      event.preventDefault();
      const nickname = nicknameForm.elements.nickname.value.trim();
      if (!nickname) return;
      try {
        const result = await api.setGuestWordfallNickname(guestToken, nickname);
        localStorage.setItem(guestNicknameKey, result?.display_name || nickname);
        guestRegistered = true;
        const localBest = Number(localStorage.getItem(localBestKey) || 0);
        if (localBest > 0) { await api.submitGuestWordfallGameScore(guestToken, localBest); localStorage.removeItem(localBestKey); }
        nicknameForm.reset(); nicknameError.hidden = true; nicknameModal.classList.remove("open"); nicknameModal.hidden = true;
        updateJoinButton(); await renderRanking();
      } catch (error) { nicknameError.textContent = error.message || "设置失败"; nicknameError.hidden = false; }
    }, { signal: events.signal });
    void (async () => {
      try { session = await api?.getSession?.() || null; } catch (_) { session = null; }
      if (session) guestRegistered = true;
      let isAdmin = false;
      if (session?.user?.id) {
        try { isAdmin = Boolean((await api?.getProfile?.(session.user.id))?.is_admin); } catch (_) {}
      }
      setupLibraryManagement(isAdmin);
      updateJoinButton(); await renderRanking();
    })();
    input.addEventListener("keydown", (event) => { if (event.key === "Escape") { typed = ""; input.value = ""; return; } if (event.key === " " && !event.ctrlKey) { event.preventDefault(); pauseButton.click(); return; } if (/^[a-zA-Z]$/.test(event.key) && !event.ctrlKey && !event.metaKey) { event.preventDefault(); acceptLetter(event.key.toLowerCase()); } }, { signal: events.signal });
    input.addEventListener("input", () => {
      const incoming = input.value.toLowerCase().replace(/[^a-z]/g, "");
      const pending = incoming.slice(typed.length);
      if (!pending) return;
      input.value = typed;
      [...pending].forEach(acceptLetter);
    }, { signal: events.signal });
    input.addEventListener("paste", (event) => { event.preventDefault(); const text = event.clipboardData.getData("text").toLowerCase().replace(/[^a-z]/g, ""); [...text].forEach(acceptLetter); }, { signal: events.signal });
    arena.addEventListener("pointerdown", () => input.focus(), { signal: events.signal });
    document.addEventListener("keydown", (event) => {
      if (document.activeElement === libraryText || libraryModal.classList.contains("open")) return;
      if (event.key === " " && document.activeElement !== input) { event.preventDefault(); pauseButton.click(); return; }
      if (/^[a-zA-Z]$/.test(event.key) && !event.ctrlKey && !event.metaKey && document.activeElement !== input) {
        const tag = document.activeElement?.tagName;
        if (["INPUT", "TEXTAREA", "SELECT"].includes(tag)) return;
        event.preventDefault();
        acceptLetter(event.key.toLowerCase());
      }
    }, { signal: events.signal });
    activeDestroy = () => { events.abort(); cancelAnimationFrame(frame); words.forEach((entry) => entry.element.remove()); arena.dataset.wordfallReady = ""; };
  }
  window.initXiaoLuoWordfall = initWordfall;
  window.destroyXiaoLuoWordfall = () => activeDestroy?.();
  document.addEventListener("DOMContentLoaded", initWordfall);
})();
